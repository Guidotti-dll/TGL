import styled from 'styled-components/native'

import {
  Container as ContainerBase,
  FilterContainer as FilterContainerBase,
} from '../RecentGames/styles'

export const Container = styled(ContainerBase)``
export const FilterContainer = styled(FilterContainerBase)``
